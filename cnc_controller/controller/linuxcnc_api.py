"""
LinuxCNC API wrapper.

Wraps the LinuxCNC Python bindings (linuxcnc.stat, linuxcnc.command)
and exposes a clean interface the rest of the application can depend on.

Install: only available on a machine running LinuxCNC.
Use mock_cnc.py for development on non-LinuxCNC hardware.
"""
# TODO: implement when running on LinuxCNC hardware
